const userRouter = require('./userRouter');
const marketplaceRouter = require('./marketplaceRouter');
const productRouter = require('./productRouter')

module.exports = { userRouter, marketplaceRouter, productRouter };
